#include <iostream>

using namespace std;

void power(int base, int exponent);

int main() {

	return 0;
}

void power(int base, int exponent) {
	int number;

	number = pow(num, 2);
	cout << "Base number: " << base << endl;
	cout << "Exponent: " << exponent << endl;
}